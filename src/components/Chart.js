//the Chart component displays all of the kits selected and allows the user to input sample IDs and cell counts to generate the corresponding constants. the Chart can then be printed. a "clear" button is provided to clear the Chart, which will also auto-clear in 24 hours.
import React, { Component } from "react";
import "./Chart.css";

class Chart extends Component {
  render() {
    return <div>chart</div>;
  }
}
export default Chart;
